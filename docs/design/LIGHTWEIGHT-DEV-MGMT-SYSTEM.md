# Workshop-Lite Design Reference

This repository consumes the Workshop-Lite substrate but does not own its
design-of-record.

The canonical design lives upstream in the Workshop-Lite repository's design
docs. This managed pointer exists so the Workshop-Lite orientation blocks in
`AGENTS.md` and `CLAUDE.md` resolve inside consumer repos without making the
consumer repo the design owner.
